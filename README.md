# Slack Simple Post Application

## Quickstart

1. Create `message.md` with markdown text
2. Set environment variables
    ```
    SLACK_TOKEN=xoxb-******
    SLACK_CHANNEL=my-channel
    ```
3. run `node index.js`
